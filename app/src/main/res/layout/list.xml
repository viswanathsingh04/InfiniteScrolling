<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:padding="5dp">

    <ImageView
        android:id="@+id/list_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="5dp"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/list_news_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/list_img"
        android:ellipsize="end"
        android:lines="2"
        android:text="@string/title"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/list_news_source"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/list_news_title"
        android:layout_toEndOf="@+id/list_img"
        android:ellipsize="end"
        android:lines="2"
        android:text="@string/News_Papaer"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/list_news_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/list_news_title"
        android:textAlignment="textEnd"
        android:layout_toEndOf="@+id/list_news_source"
        android:lines="2"
        android:text="@string/time"
        android:background="@color/colorAccent"/>

</RelativeLayout>
